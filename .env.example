# Telegram (required)
BOT_TOKEN=your-bot-token-from-botfather
WEBAPP_URL=https://vernita-unsacrificeable-eura.ngrok-free.dev

# Database (required)
DATABASE_URL=postgresql://user:password@localhost:5432/gantt_bot

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Webhook (production only — omit for dev to use long polling)
# WEBHOOK_URL=https://your-domain.com
# WEBHOOK_SECRET=a-random-string-at-least-16-chars